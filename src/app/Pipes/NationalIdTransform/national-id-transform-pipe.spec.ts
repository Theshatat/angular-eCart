import { NationalIdTransformPipe } from './national-id-transform-pipe';

describe('NationalIdTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalIdTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
